[Unit]
Description=Roda o Suricatoos health monitor periodicamente

[Timer]
# 2 min após o boot, depois a cada 3 min. Jitter evita bater sempre no mesmo instante.
OnBootSec=2min
OnUnitActiveSec=3min
RandomizedDelaySec=20
AccuracySec=15s
Persistent=true

[Install]
WantedBy=timers.target
